# Telegram Industry Analytics Bot

Телеграм-бот для получения аналитики по различным отраслям экономики с интеграцией GigaChat AI и базой данных PostgreSQL.

## Возможности

- 📊 Выбор отрасли (Финансы, IT, Энергетика)
- 💱 Отображение курсов валют
- 📰 Новости отрасли
- 📈 AI-аналитика с помощью GigaChat
- 👥 Хранение пользователей в PostgreSQL
- 🐳 Docker контейнеризация

## Быстрый старт

### Предварительные требования
- Docker и Docker Compose
- Токен Telegram бота (@BotFather)
- Токен GigaChat API

### Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/ваш-username/telegram-industry-bot.git
cd telegram-industry-bot
```

2. Настройте переменные окружения:
```bash
cp .env.example .env
# Отредактируйте .env файл
```

3. Запустите приложение:
```bash
docker-compose up -d
```

4. Бот запущен! Найдите его в Telegram и начните использовать.

## Настройка

### Получение токенов

1. **Telegram Bot Token:**
   - Напишите @BotFather в Telegram
   - Создайте нового бота с помощью `/newbot`
   - Скопируйте токен

2. **GigaChat Token:**
   - Зарегистрируйтесь на [GigaChat](https://developers.sber.ru/portal/products/gigachat)
   - Создайте приложение и получите токен

### Конфигурация базы данных

Приложение автоматически создаст необходимые таблицы при первом запуске.

## Использование

1. Запустите бота командой `/start`
2. Выберите отрасль из меню
3. Используйте кнопки для:
   - Просмотра курсов валют
   - Чтения новостей отрасли
   - Получения AI-аналитики
   - Смены отрасли

## Команды разработчика

```bash
# Запуск в development режиме
docker-compose up --build

# Просмотр логов
docker-compose logs -f bot

# Остановка
docker-compose down

# Остановка с удалением volumes
docker-compose down -v

# Миграция базы данных
docker-compose exec db psql -U postgres -d industry_bot -f /docker-entrypoint-initdb.d/init-db.sql
```

## Структура базы данных

- `users` - информация о пользователях
  - `id` - уникальный ID пользователя
  - `telegram_id` - ID в Telegram
  - `username` - имя пользователя
  - `current_industry` - выбранная отрасль
  - `created_at` - дата регистрации

## API

### Интеграции
- **Telegram Bot API** - взаимодействие с пользователями
- **GigaChat API** - генерация аналитики
- **PostgreSQL** - хранение данных пользователей

## Безопасность

- Все секреты хранятся в переменных окружения
- Используется безопасное подключение к БД
- Регулярное обновление зависимостей

## Лицензия

MIT License